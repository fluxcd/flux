// Types and procedures for representing Docker objects.
//
// These types are mainly for the purpose of turning files and
// exported bytes into resource definitions to send to
// `platform.Sync`, and ignore much of the detail in "real" Docker
// objects.

package resource
