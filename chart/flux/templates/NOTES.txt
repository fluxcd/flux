Get the Git deploy key by either (a) running

  kubectl -n {{ .Release.Namespace }} logs deployment/{{ .Release.Name }} | grep identity.pub | cut -d '"' -f2

or by (b) installing fluxctl through
https://docs.fluxcd.io/en/latest/references/fluxctl#installing-fluxctl
and running:

  fluxctl identity --k8s-fwd-ns {{ .Release.Namespace }}
{{ if eq .Values.redis.enabled true }}

WARNING
-------
You're running Redis as repository image cache backed, an experimental feature
and not yet generally available at your own risk: feel free to ping maintainers
on the WeaveCommunity Slack Workspace channel #flux.
{{- end }}
